# @hasna/banking 0.0.1 Release Evidence

Date: 2026-06-29

## Package

- Package: `@hasna/banking`
- Version: `0.0.1`
- CLI binary: `banking`
- MCP binary: `banking-mcp`
- GitHub repository: `hasna/banking`
- Visibility: public
- License: Apache-2.0

## Publish Readiness

- npm registry lookup before release: `@hasna/banking` not yet published.
- Git tags before release: no `v*` tags on `origin`.
- Bun supply-chain quarantine remains enabled globally.
- `@hasna/banking` is listed in `~/.bunfig.toml` under exact `minimumReleaseAgeExcludes` for supervised post-publish installs.

## Validation Commands

```bash
bun run verify:release
```

Result: passed with typecheck, 53 tests, 438 expect calls, build, dist smoke, and pack dry-run.

Secret-pattern scan equivalent to the CI workflow check.

Result: no matches.

## Pack Contents

`bun pm pack --dry-run --ignore-scripts` includes 33 files and an unpacked size of about 193 KB.

Important included paths:

- `dist/index.js`
- `dist/index.d.ts`
- `dist/cli/index.js`
- `dist/cli/index.d.ts`
- `dist/mcp/index.js`
- `dist/mcp/index.d.ts`
- `docs/schema/postgres.sql`
- `scripts/postinstall.ts`
- `README.md`
- `LICENSE`
- `SECURITY.md`
- `CONTRIBUTING.md`
- `CHANGELOG.md`

## Provider Release Posture

- Mercury, bunq, Revolut Business, and Erste BCR adapters are `contract_only`.
- Adapter descriptors report `liveExecution: false`.
- Adapter descriptors report `implementedOperations: []`.
- Provider operations remain non-executable until a later conformance task marks an exact operation verified.
- Mercury payments require a provider `recipientId`, idempotency key, amount >= 0.01, allowed payment method mapping, and domestic-wire purpose before future submission.
- Mercury and bunq sensitive-card-data support is disabled until exact official endpoint evidence exists.

## Provenance Plan

The first release is published from the public `main` branch after:

- all four adversarial review gates pass,
- mandatory staged secret scans pass before commit and push,
- public-tree leak scans pass,
- `bun run verify:release` passes,
- package contents are checked by pack dry-run,
- the GitHub release and npm version are recorded back to Todos and `#open-banking`.
